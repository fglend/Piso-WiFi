Flask==3.0.2
gunicorn==26.0.0
python-dotenv==1.0.0
# netifaces and psutil were listed but imported nowhere. Both are C
# extensions with no armv7 wheels, so on an Orange Pi they dragged in the
# whole build toolchain and compiled from source on every install.

# Development / testing
pytest==8.0.0
pytest-flask==1.3.0
pytest-cov==4.1.0
black==23.3.0
flake8==6.1.0
